# Filter protected models at routing time

Balance Protection will preserve the channel's configured models, abilities, and enabled status while temporarily excluding non-allowlisted models during channel selection. This keeps configuration lossless, lets free models continue through the protected channel, and allows paid models to fall through to other routes without conflating balance state with manual or automatic channel disablement.
